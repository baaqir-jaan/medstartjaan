services:
  - type: web
    name: medicare-calculator-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
